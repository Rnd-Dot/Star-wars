const UserProfile = ({data}) => {
    return <div>
        <span>{data.name}</span>
        <span>{data.name}</span>
        <span>{data.name}</span>
        <span>{data.name}</span>
        <span>{data.name}</span>
        <span>{data.name}</span>
        <span>{data.name}</span>
    </div>
}

export default UserProfile